name: Build and Push CatMcp Docker Image to ACR

on:
  push:
    paths:
      - 'MCPServers/CatMcp/**'
      - '.github/workflows/docker-publish-catmcp.yml'
      - 'Infrastructure/containerApp-CatMcp.dev.bicepparam'
    branches:
      - main
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish-dev:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/docker-publish-template.yml
    with:
      mcp_server: 'CatMcp'
      image_name: 'catmcp'
      docker_context: 'MCPServers'
      environment: dev
    secrets: inherit

  deploy-dev:
    needs: publish-dev
    if: needs.publish-dev.result == 'success'
    concurrency:
      group: azure-deploy-dev
      cancel-in-progress: false
    uses: ./.github/workflows/docker-deploy-containerapp-template.yml
    with:
      parameter_file_name: containerApp-CatMcp.dev.bicepparam
      environment: dev
    secrets: inherit

  publish-prod:
    if: github.event_name == 'release' && startsWith(github.ref, 'refs/tags/v') && startsWith(github.event.release.name, 'CatMcp')
    uses: ./.github/workflows/docker-publish-template.yml
    with:
      mcp_server: 'CatMcp'
      image_name: 'catmcp'
      docker_context: 'MCPServers'
      environment: prod
    secrets: inherit

  deploy-prod:
    needs: publish-prod
    if: needs.publish-prod.result == 'success'
    concurrency:
      group: azure-deploy-prod
      cancel-in-progress: false
    uses: ./.github/workflows/docker-deploy-containerapp-template.yml
    with:
      parameter_file_name: containerApp-CatMcp.prod.bicepparam
      environment: prod
      image_tag: ${{ needs.publish-prod.outputs.image_tag }}
    secrets: inherit
